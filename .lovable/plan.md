

## Plan: Add `.env` to `.gitignore`

Add `.env` to the existing `.gitignore` file so the environment file is excluded from Git tracking as an extra safety measure.

### Changes
- **`.gitignore`**: Add `.env` entry to prevent the file from being committed to the repository.

